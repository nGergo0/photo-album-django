apiVersion: v1
kind: Service
metadata:
  name: db-postgres
  namespace: ${namespace}
spec:
  selector:
    name: db-postgres
  ports:
    - name: postgresql
      port: 5432
      protocol: TCP
      targetPort: 5432
  type: ClusterIP
