apiVersion: v1
kind: Service
metadata:
  name: photo-album-django
  namespace: ${namespace}
spec:
  selector:
    app: photo-album-django
    deployment: photo-album-django
  ports:
    - name: 8000-tcp
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
